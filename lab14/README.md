# 🧪 JavaScript Test App + DevTools Demo

Этот проект представляет собой небольшое интерактивное приложение, демонстрирующее:

- работу функций на JavaScript (факториал, сумма массива, поиск максимума);
- наличие “сломанной” функции `findMaxBroken()` для отладки в DevTools;
- мини-тесты, которые запускаются в браузере;
- полноценные модульные тесты с использованием Jest.

Приложение полностью собрано в одном файле `index.html`, который можно запустить прямо в браузере через Visual Studio Code (например, с расширением Live Server).

---

## 📁 Структура проекта
project/
│── index.html # Основное приложение (интерактивное)
│── functions.js # Логика функций для тестов
│── functions.test.js # Модульные тесты Jest
│── package.json # Настройка тестов


---
📸 Скриншот процесса отладки:
<img width="840" height="828" alt="image" src="https://github.com/user-attachments/assets/21af18ad-5124-4710-94d2-2d96388fcea1" />
<img width="821" height="810" alt="image" src="https://github.com/user-attachments/assets/f5e980b1-c8de-4f80-8f18-268712bb5b3d" />


## 🚀 Как запустить тесты

```bash
npm install

npm test

🧠 Описание функций
✔ factorial(n)

Вычисляет факториал числа.

Пример:
Вход: 5
Выход: 120

sumArray(arr)
Возвращает сумму элементов массива.

Пример:
Вход: [1,2,3,4]
Выход: 10

findMaxBroken(arr)
Намеренно сломанная функция для демонстрации DevTools.
Из-за ошибки выбирает минимальный, а не максимальный элемент.

Ошибка в строке:

js
Copy code
if (arr[i] < max)  // должно быть >
Пример:
Вход: [3, 10, 1, 7]
Ожидаемый вывод: 10
Сломанная функция выводит: 1

✔ findMax(arr)
Исправленная версия поиска максимума.

Пример:
Вход: [3, 10, 1, 7]
Выход: 10

Примеры тестов (Jest)
Файл: functions.test.js

js
Copy code
test('Факториал 5 равен 120', () => {
  expect(factorial(5)).toBe(120);
});

test('Сумма массива [1,2,3] = 6', () => {
  expect(sumArray([1, 2, 3])).toBe(6);
});

test('Максимум массива [1,5,2] = 5', () => {
  expect(findMax([1,5,2])).toBe(5);
});
🛠 Инструкция по отладке в DevTools
Открыть страницу index.html в браузере.

Нажать F12 (DevTools).

Перейти во вкладку Sources.

Найти <script> внутри index.html.

Поставить breakpoint на строке:

js
Copy code
if (arr[i] < max)
Ввести массив в поле, например: 3,10,1,7.

Нажать кнопку "Сломанная findMaxBroken()".

Использовать:

Step Over

Step Into

Watch → добавить arr[i] и max
