# Лабораторная работа №11  
## Тема: Разработка простого одностраничного приложения (SPA) на React

---

🎯 Цель работы
Освоить основы создания одностраничных приложений с использованием **React**, научиться:
- работать с компонентами;
- использовать состояние (`useState`) и эффекты (`useEffect`);
- обрабатывать события;
- сохранять данные в `LocalStorage`.

---

🧩 Краткое описание приложения

Приложение представляет собой **список книг**, где пользователь может:
- добавлять книги (вводя название в форму);
- удалять ненужные записи;
- данные автоматически сохраняются в `LocalStorage`, поэтому список не исчезает при перезагрузке страницы.

Всё приложение реализовано в **одном файле `index.html`**, без сборщиков и дополнительных инструментов — React подключается через CDN.

---

💻 Используемые технологии

- **HTML5** — структура страницы;  
- **CSS3** — простое оформление;  
- **React 18 (CDN)** — библиотека для построения интерфейсов;  
- **ReactDOM** — для рендеринга приложения в DOM;  
- **Babel Standalone** — позволяет использовать JSX прямо в HTML-файле;  
- **LocalStorage API** — для хранения данных на стороне клиента.

---

📂 Структура проекта

project/
│
├── index.html # Основной и единственный файл приложения
└── README.md # Отчёт и описание работы


---

🧠 Логика работы приложения

1. При загрузке страницы React создаёт компонент `App`.  
2. В `App` хранится состояние `books` — список всех книг.  
   - При запуске оно читается из `localStorage`.  
3. Компонент `BookForm` позволяет добавить новую книгу.  
   - После нажатия на кнопку «Добавить» книга добавляется в список и сохраняется.  
4. Компонент `BookList` отображает список всех книг.  
   - Каждая книга имеет кнопку «Удалить», которая удаляет её из состояния.  
5. Хук `useEffect` следит за изменениями списка и обновляет `localStorage`.  

---

📸 Скриншот результата

<img width="742" height="303" alt="image" src="https://github.com/user-attachments/assets/c601fe7b-4ce1-4c00-9a12-dbfd92e15dcb" />



📜 Заключение

В ходе работы было создано простое одностраничное приложение на React, реализующее:
- использование функциональных компонентов;
- управление состоянием с помощью `useState`;
- хранение данных с помощью `localStorage`;
- динамическое обновление интерфейса при изменении данных.

Полученные навыки можно применить при разработке более сложных интерфейсов и CRUD-приложений.
